apiVersion: v1
kind: Service
metadata:
  name: todoapp-cluster-ip-service
  namespace: todoapp
spec:
  selector:
    app: todoapp
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8080
  type: ClusterIP
